const enemyTurn = function (player, enemy) {
  const enemyWeapons = enemy.weapons;
  const enemyPowers = enemy.powers;
  const enemyWeaponsOptions = enemyWeapons.length;
  const enemyPowerOptions = enemyPowers.length;

  
};
